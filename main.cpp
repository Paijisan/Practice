#include <iostream>
using namespace std;

int main()
{
    /*
        //needed variables
        //varibles year and month
        int month, year;

        //prompt user to enter a month
        cout << "Enter a month ";
        cin >> month; 
        //output that if the user is out of range
        //print error message and exit the program
        if (month < 1 or month > 12)
        cout << "Error: Invalid month entered." << endl;
        
        //prompt the user to enter a year
        //iif integer entered is negative, print error
        //message and exit the program
        cout << "Enter a year ";
        cin >> year;
        if(year < 0)
        cout << "Error: Invalid year entered."


        //if value is 1, 3, 5, 7, 8, 10, 12, then it has 31 days
        //use switch statement 
        switch(month)
        {
            case 1:case 3: case 5: case 7: case 8: case 10: case 12:
            cout << "31 days" << endl;

            break;
            //for months 4, 6, 9, 11, print 30 days
            cout << "30 days" << endl; 

            //if the month is 2, check if the year is a leap year. 
            //if it is a leap year, print 29 days, otherwise print 28 days

            case 2:

            if(year is divisible by 100 and year is divisible by 400)
            cout << "29 days" << endl

            else if*
        }


    
    
    
    
    
    
    */


    
    
    

}